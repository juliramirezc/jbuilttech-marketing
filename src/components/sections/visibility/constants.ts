/**
 * Visibility section scroll configuration.
 *
 * Pinned heights are applied via Tailwind in VisibilitySection:
 * - Mobile: min-h-[115vh]
 * - Desktop (lg+): min-h-[175vh]
 */

/** Desktop pinned section height (lg+) — do not change */
export const VISIBILITY_SECTION_HEIGHT_VH = 175;

/** Mobile pinned section height — shorter hold after social cards */
export const VISIBILITY_SECTION_HEIGHT_VH_MOBILE = 115;
